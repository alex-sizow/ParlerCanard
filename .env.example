# Telegram initData validation endpoint (optional)
# If not set, client-side validation is used as fallback
# Point this to your server/index.js endpoint
VITE_VALIDATE_URL=http://localhost:3001/api/validate
